---
import { getCollection } from "astro:content";
import FormattedDate from "../../components/FormattedDate.astro";
import { SITE_DESCRIPTION, SITE_TITLE } from "../../consts";
import BaseLayout from "../../layouts/BaseLayout.astro";

// 获取所有日记并筛选年度总结
const allDiaries = await getCollection("diary");

// 获取年度总结（标题包含"年度"、"年终"或"年总结"）
const annualDiaries = allDiaries
    .filter(
        (diary) =>
            diary.data.title.includes("年度") ||
            diary.data.title.includes("年终") ||
            diary.data.title.includes("年总结") ||
            (diary.data.title.includes("年") &&
                (diary.data.title.includes("总结") ||
                    diary.data.title.includes("回顾"))),
    )
    .sort((a, b) => b.data.pubDate.getTime() - a.data.pubDate.getTime());
---

<BaseLayout title={`年度日记 | ${SITE_TITLE}`} description={SITE_DESCRIPTION}>
    <main class="container mx-auto px-4 py-12">
        <div class="max-w-4xl mx-auto">
            <h1
                class="text-3xl font-bold mb-6 text-gray-800 dark:text-gray-100"
            >
                年度日记
            </h1>
            <p class="text-lg mb-8 text-gray-700 dark:text-gray-300">
                年度回顾与总结，记录每一年的重要成长和人生感悟。
            </p>

            {
                annualDiaries.length > 0 ? (
                    <div class="space-y-8">
                        {annualDiaries.map((diary) => (
                            <article class="bg-gradient-to-r from-purple-50 to-pink-50 dark:from-purple-900/20 dark:to-pink-900/20 p-8 rounded-xl shadow-lg hover:shadow-xl transition border border-purple-200 dark:border-purple-800">
                                <div class="flex items-start justify-between mb-6">
                                    <div class="flex-1">
                                        <h2>
                                            <a
                                                href={`/diary/annual/${diary.id}`}
                                                class="text-2xl font-bold hover:text-purple-600 dark:hover:text-purple-400 text-gray-900 dark:text-white transition-colors block mb-2"
                                            >
                                                {diary.data.title}
                                            </a>
                                        </h2>
                                        <div class="flex items-center gap-6 text-sm text-gray-600 dark:text-gray-400">
                                            <div class="flex items-center gap-2">
                                                <FormattedDate
                                                    date={diary.data.pubDate}
                                                />
                                            </div>
                                            {diary.data.mood && (
                                                <div class="flex items-center gap-2">
                                                    <span class="text-purple-600 dark:text-purple-400">
                                                        🌟 {diary.data.mood}
                                                    </span>
                                                </div>
                                            )}
                                        </div>
                                    </div>
                                    <div class="text-4xl opacity-70">🎯</div>
                                </div>

                                {diary.data.description && (
                                    <div class="mb-6">
                                        <p class="text-gray-700 dark:text-gray-300 leading-relaxed text-lg">
                                            {diary.data.description}
                                        </p>
                                    </div>
                                )}

                                {diary.data.tags &&
                                    diary.data.tags.length > 0 && (
                                        <div class="flex gap-3 flex-wrap mb-4">
                                            {diary.data.tags.map(
                                                (tag: string) => (
                                                    <span class="text-sm bg-gradient-to-r from-purple-100 to-pink-100 dark:from-purple-800 dark:to-pink-800 text-purple-800 dark:text-purple-200 px-4 py-2 rounded-full font-medium">
                                                        # {tag}
                                                    </span>
                                                ),
                                            )}
                                        </div>
                                    )}

                                <div class="flex items-center justify-between pt-4 border-t border-purple-200 dark:border-purple-700">
                                    <a
                                        href={`/diary/annual/${diary.id}`}
                                        class="inline-flex items-center gap-2 text-purple-600 dark:text-purple-400 hover:text-purple-800 dark:hover:text-purple-300 font-medium transition-colors"
                                    >
                                        阅读全文
                                        <svg
                                            class="w-4 h-4"
                                            fill="none"
                                            stroke="currentColor"
                                            viewBox="0 0 24 24"
                                        >
                                            <path
                                                stroke-linecap="round"
                                                stroke-linejoin="round"
                                                stroke-width="2"
                                                d="M9 5l7 7-7 7"
                                            />
                                        </svg>
                                    </a>
                                    <div class="text-sm text-gray-500 dark:text-gray-400">
                                        年度回顾
                                    </div>
                                </div>
                            </article>
                        ))}
                    </div>
                ) : (
                    <div class="text-center py-16">
                        <div class="text-gray-400 dark:text-gray-500 mb-6">
                            <svg
                                class="mx-auto h-16 w-16"
                                fill="none"
                                viewBox="0 0 24 24"
                                stroke="currentColor"
                            >
                                <path
                                    stroke-linecap="round"
                                    stroke-linejoin="round"
                                    stroke-width="2"
                                    d="M12 8v13m0-13V6a2 2 0 112 2h-2zm0 0V5.5A2.5 2.5 0 109.5 8H12zm-7 4h14M5 12a2 2 0 110-4h14a2 2 0 110 4M5 12v7a2 2 0 002 2h10a2 2 0 002-2v-7"
                                />
                            </svg>
                        </div>
                        <h3 class="text-xl font-medium text-gray-900 dark:text-gray-100 mb-3">
                            暂无年度总结
                        </h3>
                        <p class="text-gray-500 dark:text-gray-400 mb-6 max-w-md mx-auto">
                            还没有写过年度总结。每年结束时记录下这一年的收获与成长吧！
                        </p>
                        <div class="text-sm text-gray-600 dark:text-gray-400 bg-gradient-to-r from-purple-50 to-pink-50 dark:from-purple-900/20 dark:to-pink-900/20 p-6 rounded-lg max-w-lg mx-auto border border-purple-200 dark:border-purple-700">
                            <p class="font-medium mb-3 text-purple-800 dark:text-purple-300">
                                如何添加年度总结：
                            </p>
                            <ul class="text-left space-y-2">
                                <li class="flex items-start gap-2">
                                    <span class="text-purple-500 mt-1">•</span>
                                    <span>
                                        在{" "}
                                        <code class="bg-purple-100 dark:bg-purple-800 px-2 py-1 rounded text-xs">
                                            src/content/diary/annual/
                                        </code>{" "}
                                        目录下创建 .md 文件
                                    </span>
                                </li>
                                <li class="flex items-start gap-2">
                                    <span class="text-purple-500 mt-1">•</span>
                                    <span>
                                        标题包含"年度"、"年终"或"年总结"
                                    </span>
                                </li>
                            </ul>
                        </div>
                    </div>
                )
            }
        </div>
    </main>
</BaseLayout>
